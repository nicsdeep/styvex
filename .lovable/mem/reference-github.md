---
name: GitHub repo
description: GitHub connection for STYVEX — workspace connection nicsdeep exists; user approved creating a NEW repository via Lovable Git sync
type: reference
---
GitHub workspace connection: account `nicsdeep` (Lovable GitHub App installed, has access to nicsdeep/styvex).
User explicitly approved (2026-08-31) creating a NEW repository via Lovable's Git sync (Plus → GitHub → Connect project → Create Repository). The earlier rule requiring the existing repo nicsdeep/styvex is revoked. Connect the project by creating a new repo through the Lovable UI; the agent cannot link git itself.
